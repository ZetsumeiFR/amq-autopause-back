# Production Environment Configuration
# Copy this file to .env and fill in the values

# ===========================================
# DATABASE - PostgreSQL (managed by docker-compose)
# ===========================================
POSTGRES_PASSWORD=your_strong_password_here

# ===========================================
# AUTHENTICATION - Better Auth
# ===========================================
# Generate a secure random string (minimum 32 characters)
# Example: openssl rand -base64 32
BETTER_AUTH_SECRET=your_secret_key_minimum_32_characters_long

# ===========================================
# TWITCH OAUTH CREDENTIALS
# ===========================================
# Get these from https://dev.twitch.tv/console
TWITCH_CLIENT_ID=your_twitch_client_id
TWITCH_CLIENT_SECRET=your_twitch_client_secret

# ===========================================
# TWITCH EVENTSUB WEBHOOK SECRET
# ===========================================
# Generate a secure random string (10-100 characters)
# This is used to verify Twitch webhook signatures
TWITCH_EVENTSUB_SECRET=your_eventsub_webhook_secret

# ===========================================
# OPTIONAL SETTINGS
# ===========================================
# Sync Twitch subscriptions on startup (default: false)
SYNC_ON_STARTUP=false

# ===========================================
# NOTES:
# ===========================================
# - The API will be available at https://api.amqautopause.zetsumei.xyz
# - Webhook callback is configured to https://api.amqautopause.zetsumei.xyz/webhook/twitch
# - Make sure your Twitch app redirect URIs include:
#   - https://api.amqautopause.zetsumei.xyz/api/auth/callback/twitch
# - CORS is configured for chrome-extension:// protocol
